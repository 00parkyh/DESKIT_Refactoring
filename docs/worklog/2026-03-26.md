# 작업 로그
작성일: 2026-03-26

## 작업 목적
- 2026-03-26 대화 내용을 프로젝트 문서로 저장
- Redis 관련 추가 디버깅 대화를 별도 파일로 보관

## 수정한 파일
- `docs/conversation/2026-03-26.md`
- `src/main/java/com/deskit/deskit/ai/config/RedisVectorStoreConfig.java`
- `docs/conversation/2026-03-26_1.md`

## 수정 전 코드
```md
해당 파일 없음
```

## 수정 후 코드
```md
# 대화 기록
작성일: 2026-03-26

## 주제
- Google 서비스 계정 키 마운트 점검
- Redis 비밀번호 전달 문제 분석
- Redis protected mode 오류 추적
- Redis Stack RediSearch 명령 지원 여부 점검
```

```java
if (properties.getPassword() != null && !properties.getPassword().isEmpty()) {
    return new JedisPooled(
            new HostAndPort(properties.getHost(), properties.getPort()),
            DefaultJedisClientConfig.builder()
                    .password(properties.getPassword())
                    .build()
    );
}
```

## 수정 이유
- 오늘 진행한 설정 변경, 로그 분석, 배포 점검 대화를 추후 재확인할 수 있도록 문서로 보관하기 위해 추가했다.
- Redis 비밀번호에 URI 예약문자가 포함될 때 `redis://` 문자열 직접 조립 방식이 깨져 `null:-1` 로 연결 실패하던 문제를 방지하기 위해 Jedis client config 기반 연결로 수정했다.
- Redis 설정 수정 이후 발생한 추가 런타임 문제를 별도 대화 기록으로 남기기 위해 `_1` 파일을 추가했다.

## 추가로 해야 할 작업
- Redis 컨테이너에서 RediSearch 모듈 로딩 여부와 `FT._LIST` 지원 상태 확인
- 최신 컨테이너 로그에서 `redisVectorStore` 초기화 성공 여부 재확인
