## 작업 목적
- 판매자 방송 화면 새로고침 시 최근 채팅 내역이 비어 보이는 문제 수정

## 수정한 파일
- `front/src/lib/live/api.ts`

## 수정 전 코드
```ts
export const fetchRecentLiveChats = async (broadcastId: number, seconds = 60): Promise<LiveChatMessage[]> => {
  const { data } = await http.get<ApiResult<LiveChatMessage[]>>(`${apiBase}/livechats/${broadcastId}/recent`, { params: { seconds } })
  return ensureSuccess(data)
}
```

## 수정 후 코드
```ts
export const fetchRecentLiveChats = async (broadcastId: number, seconds = 60): Promise<LiveChatMessage[]> => {
  const { data } = await http.get<LiveChatMessage[]>(`${apiBase}/livechats/${broadcastId}/recent`, { params: { seconds } })
  return data
}
```

## 수정 이유
- `/api/livechats/{broadcastId}/recent` 백엔드는 `ApiResult`가 아니라 raw `List<LiveChatMessageDTO>`를 반환한다.
- 프론트가 이를 `ApiResult`로 파싱하면서 `ensureSuccess` 단계에서 실패했고, 호출부에서 `.catch(() => [])`로 처리되어 새로고침 직후 채팅 내역이 빈 배열로 초기화됐다.
- 실제 API 응답 형식에 맞춰 최근 채팅 복원이 정상 동작하도록 수정했다.

## 추가로 해야 할 작업
- 필요하면 최근 채팅 API를 `ApiResult`로 통일하거나, Redis miss 시 DB fallback을 추가해 복원 안정성을 높일 수 있다.

---

## 작업 목적
- 로그인하지 않은 시청자나 판매자가 공개 방송 상세 페이지를 새로고침했을 때 영상이 다시 붙지 않는 문제 수정

## 수정한 파일
- `front/src/pages/LiveDetail.vue`

## 수정 전 코드
```ts
const connectSubscriber = async (token: string) => {
  if (!viewerContainerRef.value) return
  try {
    disconnectOpenVidu()
    openviduInstance.value = new OpenVidu()
    openviduSession.value = openviduInstance.value.initSession()
```

```ts
watch(streamToken, () => {
  if (lifecycleStatus.value === 'ON_AIR') {
    void ensureSubscriberConnected()
  }
})
```

## 수정 후 코드
```ts
const waitForViewerContainer = async () => {
  if (viewerContainerRef.value) return viewerContainerRef.value
  await nextTick()
  if (viewerContainerRef.value) return viewerContainerRef.value
  await new Promise((resolve) => window.setTimeout(resolve, 50))
  return viewerContainerRef.value
}

const connectSubscriber = async (token: string) => {
  if (subscriberConnectInFlight.value) return
  const container = await waitForViewerContainer()
  if (!container) return
  subscriberConnectInFlight.value = true
  try {
    disconnectOpenVidu()
    openviduInstance.value = new OpenVidu()
    openviduSession.value = openviduInstance.value.initSession()
```

```ts
watch([lifecycleStatus, streamToken, viewerContainerRef], ([status, token, container]) => {
  if (status !== 'ON_AIR') return
  if (!token || !container) return
  void ensureSubscriberConnected()
})
```

## 수정 이유
- 공개 방송 상세 페이지는 새로고침 직후 `ON_AIR` 상태와 시청 토큰은 준비돼도, 실제 OpenVidu 비디오 컨테이너가 아직 렌더되지 않은 순간에 첫 연결을 시도할 수 있었다.
- 이 경우 `connectSubscriber`가 바로 종료되고, 이후 컨테이너가 준비되어도 재연결을 다시 시도하는 watcher가 없어 영상이 비어 있는 상태가 유지됐다.
- 컨테이너 준비를 짧게 대기한 뒤 연결하도록 하고, `lifecycleStatus`, `streamToken`, `viewerContainerRef` 조합이 모두 준비되면 재시도하도록 보강했다.

## 추가로 해야 할 작업
- 배포 후 비로그인 시청자, 로그인 회원, 판매자 계정으로 각각 방송 상세 페이지 새로고침 시 재생 복원이 동일하게 되는지 확인이 필요하다.

---

## 작업 목적
- 방송 시작 직후 판매자 송출 연결이 중복 생성되면서 OpenVidu `participantEvicted`/세션 꼬임이 발생하는 문제 완화

## 수정한 파일
- `front/src/pages/seller/LiveStream.vue`

## 수정 전 코드
```ts
const connectPublisher = async (broadcastId: number, token: string) => {
  if (openviduConnected.value) return true
  const container = await waitForPublisherContainer()
  if (!container) {
    scheduleStartRetry(broadcastId, '방송 화면 준비 중입니다. 잠시 후 다시 시도합니다.')
    return false
  }
```

```ts
const ensurePublisherConnected = async (broadcastId: number) => {
  if (openviduConnected.value) return
  await ensureLocalMediaAccess()
  await loadMediaDevices()
```

## 수정 후 코드
```ts
const connectPublisher = async (broadcastId: number, token: string) => {
  if (openviduConnected.value) return true
  if (publisherConnectInFlight.value) return false
  const container = await waitForPublisherContainer()
  if (!container) {
    scheduleStartRetry(broadcastId, '방송 화면 준비 중입니다. 잠시 후 다시 시도합니다.')
    return false
  }
  publisherConnectInFlight.value = true
```

```ts
const ensurePublisherConnected = async (broadcastId: number) => {
  if (openviduConnected.value) return
  if (publisherConnectInFlight.value) return
  await ensureLocalMediaAccess()
  await loadMediaDevices()
```

## 수정 이유
- 방송 시작 직후 `requestStartBroadcast()`가 연결을 진행하는 동안 `lifecycleStatus`/컨테이너 watcher가 `ensurePublisherConnected()`를 다시 호출할 수 있었다.
- 이 경우 동일한 방송 세션에 퍼블리셔 `connect/publish`가 겹쳐 실행되면서 OpenVidu 쪽에서 중복 퍼블리셔 정리와 `participantEvicted`가 발생할 여지가 있었다.
- 퍼블리셔 연결 중복 실행을 막는 플래그를 추가해 한 번에 하나의 `connectPublisher()`만 실행되도록 제한했다.

## 추가로 해야 할 작업
- 배포 후 판매자 방송 시작 직후 OpenVidu 콘솔 에러가 사라지는지, 시청자 새로고침 후 영상 복원이 안정화되는지 확인이 필요하다.

---

## 작업 목적
- 판매자 방송 시작 직후 호스트 토큰이 중복 발급되면서 화면이 뜨지 않는 문제 추가 완화

## 수정한 파일
- `front/src/pages/seller/LiveStream.vue`

## 수정 전 코드
```ts
const requestPublisherToken = async (broadcastId: number) => {
  if (publisherTokenInFlight.value) return null
  publisherTokenInFlight.value = true
  try {
    const token = await startSellerBroadcast(broadcastId)
```

```ts
const requestStartBroadcast = async (broadcastId: number) => {
  if (startRequested.value) return
  startRequested.value = true
  try {
    await ensureLocalMediaAccess()
    const token = await startSellerBroadcast(broadcastId)
    publisherToken.value = token
```

## 수정 후 코드
```ts
const requestPublisherToken = async (broadcastId: number) => {
  if (publisherToken.value) return publisherToken.value
  if (publisherTokenInFlight.value) return null
  publisherTokenInFlight.value = true
  try {
    const token = await startSellerBroadcast(broadcastId)
```

```ts
const requestStartBroadcast = async (broadcastId: number) => {
  if (startRequested.value) return
  startRequested.value = true
  try {
    await ensureLocalMediaAccess()
    const token = await requestPublisherToken(broadcastId)
    if (!token) {
      startRequested.value = false
      return
    }
```

## 수정 이유
- 로그상 방송 시작 직후 동일 방송에 대해 호스트 토큰이 연속으로 세 번 발급되고 있었다.
- `requestStartBroadcast()`가 직접 `startSellerBroadcast()`를 호출하고, 동시에 watcher 경로도 별도로 토큰 요청을 만들면서 중복 토큰 발급이 남아 있었다.
- 토큰 발급 경로를 `requestPublisherToken()` 하나로 통일하고, 이미 받은 토큰이 있으면 재요청하지 않도록 수정했다.

## 추가로 해야 할 작업
- 재배포 후 방송 시작 시 OpenVidu 로그에서 호스트 토큰이 1회만 발급되는지 확인이 필요하다.

---

## 작업 목적
- 퍼블리셔 재연결 과정에서 토큰을 불필요하게 초기화해 호스트 토큰이 다시 발급되는 문제 수정

## 수정한 파일
- `front/src/pages/seller/LiveStream.vue`

## 수정 전 코드
```ts
const resetOpenViduState = () => {
  openviduConnected.value = false
  publisherConnectInFlight.value = false
  publisherToken.value = null
  openviduPublisher.value = null
```

## 수정 후 코드
```ts
const resetOpenViduState = () => {
  openviduConnected.value = false
  openviduPublisher.value = null
  openviduSession.value = null
  openviduInstance.value = null
```

```ts
const resetRealtimeState = () => {
  ...
  disconnectOpenVidu()
  publisherConnectInFlight.value = false
  publisherToken.value = null
  startRequested.value = false
```

## 수정 이유
- `connectPublisher()` 시작 시 내부에서 `disconnectOpenVidu()`를 호출하는데, 이 과정이 `publisherToken`까지 즉시 초기화하고 있었다.
- 그 결과 연결이 아직 진행 중이어도 watcher가 다시 `requestPublisherToken()`을 태워 호스트 토큰을 재발급받는 문제가 생겼다.
- 토큰 초기화는 페이지 초기화나 방송 이탈 시점에만 하도록 옮겨, 재연결 중에는 기존 토큰을 유지하게 수정했다.

## 추가로 해야 할 작업
- 재배포 후 OpenVidu 로그에서 방송 시작 시 호스트 토큰 발급이 1회로 정리되는지 확인이 필요하다.

---

## 작업 목적
- 동시에 들어오는 퍼블리셔 시작 요청이 같은 토큰 발급/연결 결과를 공유하도록 보강

## 수정한 파일
- `front/src/pages/seller/LiveStream.vue`

## 수정 전 코드
```ts
const requestPublisherToken = async (broadcastId: number) => {
  if (publisherToken.value) return publisherToken.value
  if (publisherTokenInFlight.value) return null
  publisherTokenInFlight.value = true
```

```ts
const connectPublisher = async (broadcastId: number, token: string) => {
  if (openviduConnected.value) return true
  if (publisherConnectInFlight.value) return false
```

## 수정 후 코드
```ts
let publisherTokenPromise: Promise<string | null> | null = null
let publisherConnectPromise: Promise<boolean> | null = null
```

```ts
const requestPublisherToken = async (broadcastId: number) => {
  if (publisherToken.value) return publisherToken.value
  if (publisherTokenPromise) {
    return await publisherTokenPromise
  }
```

```ts
const connectPublisher = async (broadcastId: number, token: string) => {
  if (openviduConnected.value) return true
  if (publisherConnectPromise) {
    return await publisherConnectPromise
  }
```

## 수정 이유
- 기존 구조는 동시 호출이 들어오면 하나는 진행하고 나머지는 `null` 또는 `false`로 빠지면서, 다른 watcher/재시도 경로가 다시 시작 요청을 만드는 여지가 있었다.
- 토큰 발급과 퍼블리셔 연결 모두 단일 promise를 공유하게 만들어, 같은 방송 시작 시도는 하나의 OpenVidu 요청 결과를 함께 기다리도록 수정했다.

## 추가로 해야 할 작업
- 재배포 후 OpenVidu 로그에서 방송 시작 직후 `role=HOST` 토큰 발급이 1회로 줄고, 시청자 새로고침 후 영상이 정상 복원되는지 확인이 필요하다.
---

## 작업 목적
- 판매자 방송 화면에서 상세 조회 응답이 `READY`로 남아 있을 때 자동 시작이 다시 실행되며 `/start` API가 반복 호출되는 문제 수정

## 수정한 파일
- `front/src/pages/seller/LiveStream.vue`

## 수정 전 코드
```ts
const resolveDetailStatus = (detail: BroadcastDetailResponse) => {
  const normalized = normalizeBroadcastStatus(detail.status)
  if (detail.startedAt && ['READY', 'RESERVED'].includes(normalized)) {
    return 'ON_AIR'
  }
  return normalized
}
```

```ts
const scheduleAutoStart = (broadcastId: number, scheduledAtMs: number | null, status: BroadcastStatus) => {
  clearStartTimer()
  if (!scheduledAtMs || status !== 'READY') return
  const delay = Math.max(0, scheduledAtMs - Date.now())
```

## 수정 후 코드
```ts
const resolveSellerStreamStatus = (detail: BroadcastDetailResponse): BroadcastStatus => {
  const resolved = resolveDetailStatus(detail)
  if (
    ['READY', 'RESERVED'].includes(resolved) &&
    (startRequested.value || publisherToken.value || publisherTokenInFlight.value || openviduConnected.value)
  ) {
    return 'ON_AIR'
  }
  return resolved
}
```

```ts
const scheduleAutoStart = (broadcastId: number, scheduledAtMs: number | null, status: BroadcastStatus) => {
  clearStartTimer()
  if (!scheduledAtMs || status !== 'READY') return
  if (
    startRequested.value ||
    publisherToken.value ||
    publisherTokenInFlight.value ||
    publisherConnectInFlight.value ||
    openviduConnected.value
  ) {
    return
  }
  const delay = Math.max(0, scheduledAtMs - Date.now())
```

## 수정 이유
- 로그상 방송 송출이 한 번 성공한 뒤에도 `POST /api/seller/broadcasts/4/start`와 OpenVidu `HOST` 연결 토큰 발급이 반복됐다.
- 상세 조회 응답의 상태가 잠시 `READY`로 남아 있으면 `scheduleAutoStart()`가 다시 실행되어 시작 API를 재호출하는 흐름이 있었다.
- 이미 시작 요청이 진행 중이거나 퍼블리셔 연결이 살아 있는 경우에는 상태를 `ON_AIR`로 유지하고 자동 시작을 다시 태우지 않도록 보정했다.

## 추가로 해야 할 작업
- 프론트 재배포 후 OpenVidu 로그에서 `role=HOST` 토큰 발급이 다시 1회로 유지되는지 확인이 필요하다.

---

## 작업 목적
- 이미 `ON_AIR` 상태인 방송에서 OpenVidu 세션 캐시가 stale일 때 `/api/seller/broadcasts/{id}/start`가 `404`로 실패하는 문제 수정

## 수정한 파일
- `src/main/java/com/deskit/deskit/livehost/service/OpenViduService.java`

## 수정 전 코드
```java
String cachedSessionId = sessionMap.get(broadcastId);
if (cachedSessionId != null) {
    return cachedSessionId;
}
```

```java
Session session = openVidu.getActiveSession(sessionId);
if (session == null) {
    sessionId = createSession(broadcastId);
    session = openVidu.getActiveSession(sessionId);
}
```

## 수정 후 코드
```java
private Session getActiveSessionSafely(String sessionId) {
    if (sessionId == null || sessionId.isBlank()) {
        return null;
    }
    try {
        return openVidu.getActiveSession(sessionId);
    } catch (Exception e) {
        log.warn("OpenVidu active session lookup failed: sessionId={}, message={}", sessionId, e.getMessage());
        return null;
    }
}
```

```java
String cachedSessionId = sessionMap.get(broadcastId);
if (cachedSessionId != null) {
    Session cachedSession = getActiveSessionSafely(cachedSessionId);
    if (cachedSession != null) {
        return cachedSessionId;
    }
    sessionMap.remove(broadcastId);
}
```

```java
Session session = getActiveSessionSafely(sessionId);
if (session == null) {
    sessionMap.remove(broadcastId);
    sessionId = createSession(broadcastId);
    session = getActiveSessionSafely(sessionId);
}

if (session == null) {
    throw new IllegalStateException("OpenVidu session is not active: " + buildSessionId(broadcastId));
}
```

## 수정 이유
- 로그상 판매자 방송 시작 API가 반복 호출될 때 `BroadcastService.startBroadcast()`는 `ON_AIR` 분기에서 OpenVidu 토큰만 재발급하려고 했다.
- 이때 `OpenViduService.sessionMap`에 남아 있던 세션 ID가 stale이면 `getActiveSession`/`createConnection` 단계에서 `404`가 발생했다.
- stale 세션 캐시를 검증하고, 유효하지 않으면 캐시를 비운 뒤 세션을 다시 조회/재생성하도록 보정했다.

## 추가로 해야 할 작업
- 백엔드 재배포 후 판매자 방송 시작 시 `deskit-app`의 `OpenVidu error during broadcast start: ... 404` 로그가 사라지는지 확인이 필요하다.

---

## 작업 목적
- 시청자 방송 상세 페이지에서 같은 OpenVidu subscriber 토큰으로 중복 연결이 발생해 `401 invalid token`이 나는 문제 수정

## 수정한 파일
- `front/src/pages/LiveDetail.vue`

## 수정 전 코드
```ts
const connectSubscriber = async (token: string) => {
  const container = await waitForViewerContainer()
  if (!container) return
  try {
    disconnectOpenVidu()
    openviduInstance.value = new OpenVidu()
    openviduSession.value = openviduInstance.value.initSession()
    ...
    await openviduSession.value.connect(token)
    openviduConnectionId.value = openviduSession.value.connection?.connectionId ?? null
    openviduConnected.value = true
  } catch {
    disconnectOpenVidu()
  }
}

const ensureSubscriberConnected = async () => {
  if (!streamToken.value || lifecycleStatus.value !== 'ON_AIR') return
  if (openviduConnected.value) return
  await connectSubscriber(streamToken.value)
}
```

## 수정 후 코드
```ts
let subscriberConnectPromise: Promise<boolean> | null = null
let subscriberConnectingToken: string | null = null
let subscriberConnectedToken: string | null = null

const connectSubscriber = async (token: string) => {
  if (openviduConnected.value && subscriberConnectedToken === token) return true
  if (subscriberConnectPromise && subscriberConnectingToken === token) {
    return await subscriberConnectPromise
  }

  subscriberConnectingToken = token
  subscriberConnectInFlight.value = true
  subscriberConnectPromise = (async () => {
    const container = await waitForViewerContainer()
    if (!container) return false
    try {
      disconnectOpenVidu()
      openviduInstance.value = new OpenVidu()
      openviduSession.value = openviduInstance.value.initSession()
      ...
      await openviduSession.value.connect(token)
      openviduConnectionId.value = openviduSession.value.connection?.connectionId ?? null
      openviduConnected.value = true
      subscriberConnectedToken = token
      return true
    } catch {
      disconnectOpenVidu()
      return false
    } finally {
      subscriberConnectInFlight.value = false
      subscriberConnectingToken = null
      subscriberConnectPromise = null
    }
  })()

  return await subscriberConnectPromise
}

const ensureSubscriberConnected = async () => {
  if (!streamToken.value || lifecycleStatus.value !== 'ON_AIR') return
  if (openviduConnected.value && subscriberConnectedToken === streamToken.value) return
  if (subscriberConnectInFlight.value) return
  await connectSubscriber(streamToken.value)
}
```

## 수정 이유
- OpenVidu 서버 로그에서 같은 시청자 토큰으로 첫 번째 `joinRoom`은 성공하고, 바로 두 번째 `joinRoom`이 같은 토큰으로 다시 호출되며 `401 invalid token`이 발생했다.
- 기존 구현은 컨테이너 렌더를 기다리는 동안 중복 호출을 막지 못해 watcher 두 경로가 동시에 `connectSubscriber()`를 실행할 수 있었다.
- 토큰 단위 promise 공유와 연결 상태 기록을 추가해 같은 토큰으로는 한 번만 연결하도록 보정했다.

## 추가로 해야 할 작업
- 프론트 재배포 후 OpenVidu 서버 로그에서 같은 subscriber 토큰으로 두 번째 `joinRoom`이 사라졌는지 확인이 필요하다.

---

## 작업 목적
- 시청자 새로고침 후 OpenVidu subscriber 연결 실패 시 이미 소모된 토큰을 재사용하지 않도록 보정

## 수정한 파일
- `front/src/pages/LiveDetail.vue`

## 수정 전 코드
```ts
const requestJoinToken = async () => {
  if (!broadcastId.value) return
  if (lifecycleStatus.value !== 'ON_AIR') return
  if (joinInFlight.value) return
  if (joinedBroadcastId.value === broadcastId.value) return
  joinInFlight.value = true
  try {
    streamToken.value = await joinBroadcast(broadcastId.value, viewerId.value)
    joinedBroadcastId.value = broadcastId.value
  } finally {
    joinInFlight.value = false
  }
}

const connectSubscriber = async (token: string) => {
  try {
    ...
    await openviduSession.value.connect(token)
    openviduConnected.value = true
    return true
  } catch {
    disconnectOpenVidu()
    return false
  }
}
```

## 수정 후 코드
```ts
const invalidateJoinToken = (token?: string | null) => {
  if (token && streamToken.value && streamToken.value !== token) {
    return
  }
  streamToken.value = null
  joinedBroadcastId.value = null
}

const requestJoinToken = async () => {
  if (!broadcastId.value) return
  if (lifecycleStatus.value !== 'ON_AIR') return
  if (joinInFlight.value) return
  if (joinedBroadcastId.value === broadcastId.value && streamToken.value) return
  joinInFlight.value = true
  try {
    streamToken.value = await joinBroadcast(broadcastId.value, viewerId.value)
    joinedBroadcastId.value = broadcastId.value
  } finally {
    joinInFlight.value = false
  }
}

const connectSubscriber = async (token: string) => {
  try {
    ...
    await openviduSession.value.connect(token)
    openviduConnected.value = true
    subscriberConnectedToken = token
    return true
  } catch {
    disconnectOpenVidu()
    invalidateJoinToken(token)
    return false
  }
}

watch(streamToken, () => {
  if (lifecycleStatus.value === 'ON_AIR') {
    if (!streamToken.value) {
      void requestJoinToken()
      return
    }
    void ensureSubscriberConnected()
  }
})
```

## 수정 이유
- 새로고침 직후 OpenVidu 연결이 한 번 실패하면 기존 구현은 이미 소모되었을 수 있는 `streamToken`을 그대로 유지한 채 같은 토큰으로 다시 연결을 시도했다.
- OpenVidu subscriber 토큰은 재사용 실패 가능성이 커서, 실패한 토큰은 즉시 무효화하고 `joinBroadcast()`를 다시 호출해 새 토큰을 받아야 한다.
- `joinedBroadcastId`만 남아 있으면 새 토큰 발급 자체가 막히므로, 토큰 무효화 시 입장 상태도 함께 초기화하도록 보정했다.

## 추가로 해야 할 작업
- 프론트 재배포 후 시청자 새로고침 시 `joinBroadcast()`가 새 토큰을 다시 받고 영상이 복원되는지 확인이 필요하다.

---

## 작업 목적
- 시청자 새로고침 직후 방송 상세 응답 상태가 잠시 `READY`로 흔들리며 OpenVidu 연결을 끊는 문제 보정

## 수정한 파일
- `front/src/pages/LiveDetail.vue`

## 수정 전 코드
```ts
const loadDetail = async () => {
  if (!broadcastId.value) return
  try {
    const detail = await fetchPublicBroadcastDetail(broadcastId.value)
    liveItem.value = buildLiveItem(detail)
  } catch {
    liveItem.value = null
  }
}
```

```ts
watch(
  lifecycleStatus,
  () => {
    void requestJoinToken()
    if (lifecycleStatus.value === 'ON_AIR') {
      void ensureSubscriberConnected()
      return
    }
    disconnectOpenVidu()
  },
)
```

## 수정 후 코드
```ts
const resolveDetailStatus = (detail: BroadcastDetailResponse) => {
  const normalized = normalizeBroadcastStatus(detail.status)
  if (detail.startedAt && ['READY', 'RESERVED'].includes(normalized)) {
    return 'ON_AIR'
  }
  return normalized
}

const resolveViewerStreamStatus = (detail: BroadcastDetailResponse): BroadcastStatus => {
  const resolved = resolveDetailStatus(detail)
  if (
    ['READY', 'RESERVED'].includes(resolved) &&
    (
      joinInFlight.value ||
      subscriberConnectInFlight.value ||
      Boolean(streamToken.value) ||
      Boolean(joinedBroadcastId.value) ||
      openviduConnected.value
    )
  ) {
    return 'ON_AIR'
  }
  return resolved
}

const loadDetail = async () => {
  if (!broadcastId.value) return
  try {
    const detail = await fetchPublicBroadcastDetail(broadcastId.value)
    liveItem.value = buildLiveItem({
      ...detail,
      status: resolveViewerStreamStatus(detail),
    })
  } catch {
    liveItem.value = null
  }
}
```

## 수정 이유
- OpenVidu 서버 로그상 새로고침 후 시청자는 새 토큰으로 정상 재입장하고 `receiveVideoFrom`, `VIDEO FLOWING`까지 성공했다.
- 그런데 프론트는 상세 조회 응답의 상태가 잠시 `READY`로 내려오면 `lifecycleStatus` watcher에서 `disconnectOpenVidu()`를 호출해 스스로 영상을 끊을 수 있었다.
- 입장 중이거나 subscriber 연결이 살아 있는 동안에는 상세 응답 상태가 잠깐 흔들려도 `ON_AIR`로 유지하도록 보정했다.

## 추가로 해야 할 작업
- 프론트 재배포 후 시청자 새로고침 시 플레이어가 placeholder로 돌아가지 않고 영상이 계속 유지되는지 확인이 필요하다.

---

## 작업 목적
- 시청자 페이지에서 OpenVidu subscriber 연결은 성공하지만 모바일 autoplay 정책 때문에 실제 영상 재생이 시작되지 않는 문제 보정

## 수정한 파일
- `front/src/pages/LiveDetail.vue`

## 수정 전 코드
```ts
const applySubscriberVolume = () => {
  const container = stageRef.value
  if (!container) return
  const video = container.querySelector('video') as HTMLVideoElement | null
  if (!video) return
  video.muted = false
  video.volume = Math.min(1, Math.max(0, volume.value / 100))
}
```

## 수정 후 코드
```ts
const viewerPlaybackActivated = ref(false)

const syncSubscriberPlayback = async () => {
  const container = stageRef.value
  if (!container) return
  const video = container.querySelector('video') as HTMLVideoElement | null
  if (!video) return
  video.autoplay = true
  video.playsInline = true
  const shouldMute = !viewerPlaybackActivated.value || volume.value <= 0
  video.defaultMuted = shouldMute
  video.muted = shouldMute
  video.volume = Math.min(1, Math.max(0, volume.value / 100))
  if (video.paused) {
    await video.play().catch(() => {})
  }
}

const applySubscriberVolume = () => {
  void syncSubscriberPlayback()
}

const handleViewerPlaybackActivation = () => {
  if (viewerPlaybackActivated.value) {
    return
  }
  viewerPlaybackActivated.value = true
  void syncSubscriberPlayback()
}
```

## 수정 이유
- 브라우저 콘솔에서 subscriber 연결과 ICE connected 상태까지는 성공했지만 화면이 보이지 않았다.
- 모바일/모바일 에뮬레이션 환경에서는 unmuted autoplay가 자주 차단되므로, 기존처럼 `video.muted = false`를 바로 강제하면 실제 재생이 시작되지 않을 수 있다.
- 초기엔 muted autoplay로 재생을 붙이고, 사용자가 플레이어를 터치하면 그때 음소거를 해제할 수 있도록 보정했다.

## 추가로 해야 할 작업
- 프론트 재배포 후 시청자 새로고침 시 영상이 자동으로 보이는지, 필요하면 첫 클릭 후 음성이 정상 재생되는지 확인이 필요하다.
